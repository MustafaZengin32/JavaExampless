package incrementdecrement;

public class IncrementDecrementDeutschInterviewFragen {
    /*
1.Was ist der Unterschied zwischen dem Präinkrement-Operator (++x) und dem Postinkrement-Operator (x++)?
Antwort: Der Präinkrement-Operator erhöht den Wert der Variablen x um eins und gibt den inkrementierten Wert zurück,
während der Postinkrement-Operator den Wert von x zuerst zurückgibt und dann den Wert um eins erhöht.

2.Was ist der Wert von x nach der Ausführung des folgenden Codes?

int x = 5;
int y = ++x + x++; //y 12 x 6 x 7
Antwort: Der Wert von x wird 7 sein.
Bei ++x wird x vor dem Hinzufügen um eins inkrementiert,
dann wird x++ den Wert von x zurückgeben (jetzt 6) und anschließend wird x um eins erhöht.

3.Erklären Sie den Unterschied zwischen i++ und ++i in einer Schleife.

In einer Schleife führt i++ dazu, dass der Wert zuerst verwendet und dann inkrementiert wird (nach Schleifendurchlauf),
während ++i zuerst inkrementiert und dann verwendet wird (vor Schleifendurchlauf).







     */
}
