package set;

public class Set07DeutschInterviewFragen {
    /*

1. Was ist ein Set in Java?

Ein Set ist ein Interface in Java, das Teil des Java Collections Frameworks ist.
Es wird verwendet, um eine Sammlung von Elementen zu speichern,
erlaubt jedoch im Gegensatz zu einer List keine Duplikate.

2. Welche sind die wichtigsten Implementierungen des Set-Interfaces in Java?

Die Hauptimplementierungen des Set-Interfaces in Java sind:
HashSet: Es verwendet eine Hashtabelle zur Speicherung und garantiert keine bestimmte Reihenfolge der Elemente.
LinkedHashSet: Es behält die Reihenfolge der Elemente bei, wie sie hinzugefügt wurden.
TreeSet: Es speichert Elemente in sortierter Reihenfolge
(natürliche Reihenfolge oder mit einem benutzerdefinierten Comparator).

3. Wie stellen Sie sicher, dass ein Set keine Duplikate zulässt?

Das Set-Interface selbst erlaubt keine Duplikate.
Die Einzigartigkeit wird durch die hashCode- und equals-Methoden der Elemente sichergestellt.

5. Wie durchlaufen Sie die Elemente in einem Set?

Sie können einen Iterator oder die erweiterte for-Schleife
(for-each-Schleife) verwenden, um die Elemente in einem Set zu durchlaufen.

6. Was ist der Unterschied zwischen den Methoden add() und addAll() zum Hinzufügen von Elementen zu einem Set?

Die Methode add() wird verwendet, um ein einzelnes Element zu einem Set hinzuzufügen.
Die Methode addAll() wird verwendet, um alle Elemente aus einer anderen Sammlung zu einem Set hinzuzufügen.

7. Wie prüfen Sie, ob ein bestimmtes Element in einem Set existiert?

Sie können die Methode contains(Object obj) verwenden, um zu überprüfen,
ob ein bestimmtes Element in einem Set vorhanden ist.

8. Kann ein null-Wert zu einem Set hinzugefügt werden?

Ja, die meisten Implementierungen von Set in Java erlauben das Hinzufügen eines null-Werts.

10. Was ist der Unterschied zwischen einem Set und einer List?
- Ein Set erlaubt keine Duplikate und garantiert keine bestimmte Reihenfolge,
während eine List Duplikate erlaubt und die Reihenfolge beibehält, in der die Elemente hinzugefügt wurden.



     */
}
