package ifstatement;

public class IfStatementDeutschInterviewFragen {
    /*
Frage 1: Was ist der Zweck der "if-Anweisung" in Java?

Antwort: Die "if-Anweisung" in Java ermöglicht es uns, Entscheidungen in unserem Code zu treffen.
Mit der "if-Anweisung" können wir eine Bedingung überprüfen und abhängig vom Ergebnis der Bedingung bestimmte Codeblöcke ausführen.

Frage 2: Wie lautet die Syntax einer "if-Anweisung" in Java?
Antwort: Die Syntax einer "if-Anweisung" in Java ist wie folgt:

if (Bedingung) {
    // Codeblock, der ausgeführt wird, wenn die Bedingung wahr ist (true)
}

Frage 3: Gibt es einen Unterschied zwischen "if" und "else if"?

Antwort: Ja, es gibt einen Unterschied. "if" wird verwendet, um eine Bedingung zu überprüfen, und wenn sie wahr ist,
wird der entsprechende Codeblock ausgeführt.
"else if" wird verwendet, um eine zusätzliche Bedingung zu überprüfen, falls die vorherigen Bedingungen nicht erfüllt wurden.

Frage 4: Was passiert, wenn keine der Bedingungen in einer "if-else if"-Kaskade erfüllt ist?

Antwort: Wenn keine der Bedingungen in einer "if-else if"-Kaskade erfüllt ist, wird der Codeblock im "else"-Teil ausgeführt (sofern vorhanden).
Wenn es keinen "else"-Teil gibt, wird der Programmfluss nach der "if-else if"-Kaskade(akis) fortgesetzt.

Frage 5: Können mehrere "else if"-Blöcke in einer "if-Anweisung" verwendet werden?

Antwort: Ja, mehrere "else if"-Blöcke können in einer "if-Anweisung" verwendet werden, um mehrere Bedingungen nacheinander zu überprüfen
und entsprechende Codeblöcke auszuführen.

Frage 6: Was ist der Unterschied zwischen "==" und "equals()" in einer "if-Anweisung"?

Antwort: In einer "if-Anweisung" wird "==" verwendet, um die Gleichheit der Referenzen bei Objekten zu überprüfen,
während "equals()" verwendet wird, um den inhaltlichen Wert von Objekten zu vergleichen.
Für primitiven Datentypen wie int ist "==" angemessen,
aber für Objekte wie Strings sollte "equals()" verwendet werden, um den Inhalt zu vergleichen.
     */
}
