package statickeyword;

public class Static04DeutschInterviewFragen {

    /*
1. Was bedeutet das Schlüsselwort "static" in Java?

Antwort: Das Schlüsselwort "static" in Java wird verwendet,
um ein Element (Variable, Methode oder Block) als Teil der Klasse
und nicht als Teil einer Instanz (Objekt) zu deklarieren.

2. Wann verwenden Sie das Schlüsselwort "static" für Variablen?

Antwort: Das Schlüsselwort "static" wird für Klassenvariablen verwendet.
Klassenvariablen sind gemeinsame Variablen für alle Instanzen einer Klasse
und werden mit dem Klassennamen, gefolgt von einem Punkt, referenziert.
Sie werden einmal initialisiert und behalten ihren Wert über alle Instanzen hinweg bei.

3. Was sind die Vorteile der Verwendung von "static" in Java?

Antwort: Die Verwendung von "static" in Java bietet mehrere Vorteile:

Speicherplatzeffizienz: Statische Variablen und Methoden werden nur einmal im Speicher gespeichert,
unabhängig von der Anzahl der Klasseninstanzen.
Globale Erreichbarkeit: Statische Elemente können über den Klassennamen von überall im Programmcode aus aufgerufen werden.
Klassenbezogene Operationen: Statische Methoden können auf Klassenebene arbeiten und haben keinen Zugriff auf Instanzdaten.


     */
}
