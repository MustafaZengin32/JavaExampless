package encaptulations;

public class Encapsulation04DeutschInterviewFragen {
    /*

Was ist Kapselung in Java?

Kapselung ist ein Konzept, bei dem Daten (Variablen) und Methoden (Funktionen) in einer Klasse zusammengefasst werden
und der Zugriff auf diese Daten und Methoden von außerhalb der Klasse eingeschränkt ist.

Welches Schlüsselwort wird verwendet, um private Datenfelder in Java zu deklarieren?

Das Schlüsselwort private wird verwendet, um private Datenfelder in Java zu deklarieren,
wodurch der direkte Zugriff von außerhalb der Klasse verhindert wird.

Warum ist die Kapselung wichtig?

Kapselung verbessert die Sicherheit und Wartbarkeit von Code,
da sie den direkten Zugriff auf interne Daten und Methoden einschränkt.
Sie ermöglicht auch die Implementierung von Getter- und Setter-Methoden zur Kontrolle des Zugriffs
und zur Validierung von Daten.

Was sind Getter- und Setter-Methoden?

Getter-Methoden werden verwendet, um den Wert privater Datenfelder abzurufen.
Setter-Methoden werden verwendet, um den Wert privater Datenfelder festzulegen.
Sie können auch zur Validierung von Eingaben verwendet werden.

Können private Datenfelder von außerhalb der Klasse zugegriffen werden?

Nein, private Datenfelder können nicht direkt von außerhalb der Klasse zugegriffen werden.
Sie können nur über die öffentlichen Getter- und Setter-Methoden
oder andere öffentliche Methoden der Klasse zugegriffen werden.




     */
}
